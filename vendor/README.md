# Vendor

The build script downloads Squirrel 1.1.2 here when `vendor/Squirrel-1.1.2.pkg` is missing.

Default source:

```text
https://github.com/rime/squirrel/releases/download/1.1.2/Squirrel-1.1.2.pkg
```

The downloaded package is ignored by Git.
